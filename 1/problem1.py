print("""Tower of Hanoi
Problem Statement
You are given three pegs: A (Source), B (Auxiliary), and C (Destination), along with n disks of different sizes. Initially, all disks are stacked on peg A in decreasing order of size, with the largest disk at the bottom and the smallest at the top.

Your task is to move all n disks from peg A to peg C using peg B, adhering to the following rules:

Only one disk can be moved at a time.

A move consists of taking the top disk from one peg and placing it on another peg.

No disk may be placed on top of a smaller disk.

Function Signature
Implement a function that prints or returns the sequence of moves.

Input: An integer n representing the number of disks.

Output: A sequence of moves in the format: Move disk [disk_number] from [source_peg] to [destination_peg].""")